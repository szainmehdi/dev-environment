## Symlinking the Config File

To symlink the `config` file from this repository to your Ghostty configuration directory:

```bash
# from this directory:
mkdir -p ~/.config/ghostty
ln -s $(pwd)/config ~/.config/ghostty/config
```
